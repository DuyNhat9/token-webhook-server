providers = ["python"]

[phases.setup]
nixPkgs = ["chromium", "chromedriver"]

[phases.install]
cmds = ["/usr/bin/python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Python build completed'"]

[start]
cmd = "/usr/bin/python3 smart-token-server.py"

[variables]
CHROME_BIN = "/usr/bin/chromium"
CHROMEDRIVER_PATH = "/usr/bin/chromedriver"
